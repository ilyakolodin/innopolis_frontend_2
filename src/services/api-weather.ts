import {METRIC} from '../data/metrics'


export const ServiceWeather = (place: string, data: {statrtDate: string, endDate: string}, metric: METRIC) => {
    //

    return (
        [{
            date: '12.12.2023',
            temp: '30'
        }]
    )
}
